<?xml version="1.0" encoding="utf-8"?>
<!--
  Network Security Config — allows HTTP to local backend IPs.
  Android 9+ blocks HTTP by default; this explicitly allows it
  for our local hotspot IPs (10.x.x.x, 192.168.x.x).
-->
<network-security-config>
    <base-config cleartextTrafficPermitted="true">
        <trust-anchors>
            <certificates src="system" />
        </trust-anchors>
    </base-config>

    <!-- Allow cleartext to ALL local network IPs -->
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="true">10.0.0.0</domain>
        <domain includeSubdomains="true">192.168.0.0</domain>
        <domain includeSubdomains="true">172.16.0.0</domain>
        <domain includeSubdomains="true">localhost</domain>
    </domain-config>
</network-security-config>
